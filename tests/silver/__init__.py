"""
Tests for the silver layer.

Silver is owned by dbt — there are no Python Service classes to unit test.
Integration tests for dbt models run via `dbt test --select silver.*`
and are not part of the pytest suite.
"""
